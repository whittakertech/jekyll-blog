# Builds in Actions produce a downloadable artifact, not a hosted Pages site.
baseurl: ""
url: "http://localhost:8000"
show_drafts: true
future: true

defaults:
  - scope:
      path: ""
    values:
      robots: "noindex, nofollow"
